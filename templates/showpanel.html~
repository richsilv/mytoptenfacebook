{% macro acc_header(number, title, artist, reason) -%}
    <h3 class="header{% if newsong %} newsong{% endif %}" id="{{ number }}header">
        <span class="songtitleartist">
            <strong>{{ title }}</strong> by <strong>{{ artist }}</strong><input type="hidden" class="currenttitle" value="{{ title }}"><input type="hidden" class="currentartist" value="{{ artist }}">{% endif %}
            <span class="reason"><div>{{ reason }}</div></span>        
        </span>
    </h3>
{% endmacro %}

{% macro acc_panel(number) -%}
    <div class="panel" id="{{ number }}panel">
    </div>
{% endmacro %}
