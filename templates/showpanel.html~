{% macro acc_header(number, title, artist) -%}
    <h3 class="header{% if newsong %} newsong{% endif %}" id="{{ number }}header">
        <span class="songtitleartist">
            <strong>{{ title }}</strong> by <strong>{{ artist }}</strong><input type="hidden" class="currenttitle" value="{{ title }}"><input type="hidden" class="currentartist" value="{{ artist }}">
        </span>
    </h3>
{% endmacro %}

{% macro acc_panel(number, reason) -%}
    <div class="showpanel" id="{{ number }}panel">
        <span class="reason">{{ reason }}
            <span class="slider ui-state-default ui-corner-all"><h3>Player</h3></span>                
        </span>
        <span id="play{{ number }}" class="playerbox">
            <div class="player"></div>
        </span>
    </div>
{% endmacro %}
